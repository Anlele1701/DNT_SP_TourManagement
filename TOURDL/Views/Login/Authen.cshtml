@model TOURDL.Models.NHANVIEN

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Authen</title>
    <link rel="stylesheet" href="~/CSS/Login.css"/>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
@*<body>
    @using (Html.BeginForm("Authen", "Login", FormMethod.Post))
    {
        @Html.LabelFor(m => m.Mail_NV)
        @Html.TextBoxFor(m => m.Mail_NV)

        @Html.LabelFor(m => m.MatKhau)
        @Html.TextBoxFor(m => m.MatKhau)

        @Html.ValidationMessageFor(m => m.MatKhau)
        @Html.AntiForgeryToken()
        <input class="btn btn-primary" type="submit" value="login" />}
</body>*@
<body>
    @using (Html.BeginForm("Authen", "Login", FormMethod.Post))
    {
        <div class="container">
            <div class="card">
                <h2>Đăng Nhập</h2>
                <form>
                    <label for="username">Username</label>
                    <p>@Html.TextBoxFor(m=>m.Mail_NV)</p>
                    <label for="password">Password</label>
                    <p>@Html.TextBoxFor(m=>m.MatKhau)</p>

                    @Html.ValidationMessageFor(m => m.MatKhau)
                    @Html.AntiForgeryToken()
                    <button value="login" type="submit">Login</button>
                </form>
            </div>
        </div>
    }
</body>
</html>
